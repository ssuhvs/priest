server:
  port: 8080
  servlet:
    context-path: /

spring:
  messages:
    basename: classpath*:i18n/messages

  thymeleaf:
    prefix: classpath:/templates/
    cache: false
    excluded-view-names: [/admin/*,/jsp/*]

  freemarker:
    template-loader-path: classpath:/ftls/
    suffix: .ftl
    view-names: /test/*



zookeeper:
    url: ${priest.dubbo.zk.url}

